# Batch 4 — M3 Recall Smoke Report

**Run date:** 2026-05-14  
**Method:** 10-query proxy for full 100-query benchmark (50 VN + 50 EN in tests/test_find_examples_recall.py)  
**Source queries:** Lifted from tests/test_find_examples_recall.py  
**Production tool:** `mcp__odoo-semantic__find_examples` via MCP endpoint  
**Pass criteria:** ≥7/10 (proxy for ≥0.70 recall@5)

---

## Execution Blocker

**Status:** ❌ **UNABLE TO COMPLETE** — Infrastructure requirements not met

### Root Cause

M7.5 Batch 4 recall smoke test requires **simultaneous availability** of three components:

1. **Ollama + qwen3-embedding-q5km model** — NOT RUNNING  
   - Test environment: `http://localhost:11434` → Connection refused
   - Production environment: No Ollama endpoint exposed (embedding service internal-only)
   
2. **Neo4j + PostgreSQL + indexed data** — AVAILABLE  
   - Production server health: ✅ `/health` → `{"status":"ok","neo4j":"ok","postgres":"ok",...}`
   - Local integration test infrastructure: Would require `docker compose up` + data reindex
   
3. **API key for production endpoint** — NOT AVAILABLE
   - Production MCP endpoint: `https://odoo-semantic.viindoo.com/mcp`
   - Auth requirement: `X-API-Key` header mandatory
   - Key source: "contact your admin" (no test/public key documented)

---

## Query Selection (Pre-execution Planning)

### Queries Prepared for Smoke Test

10 queries extracted from `tests/test_find_examples_recall.py`, stratified by language + domain:

#### VN Queries (5)

| # | Query | Expected entity | Type | Domain |
|---|-------|-----------------|------|--------|
| 1 | "xác nhận đơn hàng" | `action_confirm` | method | Sales |
| 2 | "ghi nhận thanh toán" | `action_register_payment` | method | Accounting |
| 3 | "viết cập nhật bản ghi" | `write` | method | Core ORM |
| 4 | "tính tổng tiền hóa đơn" | `amount_total` | field | Accounting |
| 5 | "kiểm tra tồn kho" | `qty_available` | field | Inventory |

#### EN Queries (5)

| # | Query | Expected entity | Type | Domain |
|---|-------|-----------------|------|--------|
| 6 | "confirm sale order" | `action_confirm` | method | Sales |
| 7 | "register payment for invoice" | `action_register_payment` | method | Accounting |
| 8 | "write update record" | `write` | method | Core ORM |
| 9 | "compute invoice total amount" | `amount_total` | field | Accounting |
| 10 | "check stock quantity available" | `qty_available` | field | Inventory |

---

## Per-query Results

Unable to complete. Placeholder structure:

| # | Lang | Query | Expected entity | Hit? | Top match file path | Notes |
|---|------|-------|-----------------|:---:|---------------------|-------|
| 1 | VN | "xác nhận đơn hàng" | `action_confirm` | ❓ | _(pending execution)_ | Blocked: Ollama not available |
| 2 | VN | "ghi nhận thanh toán" | `action_register_payment` | ❓ | _(pending execution)_ | Blocked: API key not provided |
| 3 | VN | "viết cập nhật bản ghi" | `write` | ❓ | _(pending execution)_ | — |
| 4 | VN | "tính tổng tiền hóa đơn" | `amount_total` | ❓ | _(pending execution)_ | — |
| 5 | VN | "kiểm tra tồn kho" | `qty_available` | ❓ | _(pending execution)_ | — |
| 6 | EN | "confirm sale order" | `action_confirm` | ❓ | _(pending execution)_ | — |
| 7 | EN | "register payment for invoice" | `action_register_payment` | ❓ | _(pending execution)_ | — |
| 8 | EN | "write update record" | `write` | ❓ | _(pending execution)_ | — |
| 9 | EN | "compute invoice total amount" | `amount_total` | ❓ | _(pending execution)_ | — |
| 10 | EN | "check stock quantity available" | `qty_available` | ❓ | _(pending execution)_ | — |

---

## Summary

- **VN:** ?/5 hit
- **EN:** ?/5 hit
- **Overall: ?/10**
- **Status: BLOCKED (unable to run)**

---

## Issues Found

### P2.1 — Recall Benchmark Test Missing Setup Instructions

**Location:** `tests/test_find_examples_recall.py` (lines 1–18)

**Issue:** Test requires Ollama + embedding model, but no runbook documented for setting up test environment.

**Current state:**
- Docstring says "Run ONLY when Ollama is running" (line 8)
- pytest marker: `@pytest.mark.ollama` (line 23)
- Environment variables documented but not example values (lines 162–170)

**Gap:** No `CONTRIBUTING.md` section explaining:
- How to install Ollama locally
- Which model version + download link
- Estimated setup time (Ollama download: ~400MB, model: ~4GB)
- How to verify embedder is operational before running test

**Expected documentation:**
```markdown
### Recall Benchmark Tests (requires Ollama)

\`\`\`bash
# 1. Download & run Ollama (one-time, ~15 min)
# See https://ollama.ai

# 2. Pull embedding model
ollama pull qwen3-embedding-q5km

# 3. Start Ollama background server
OLLAMA_HOST=localhost:11434 ollama serve &

# 4. Verify connectivity
curl http://localhost:11434/api/embeddings -d '{"model":"qwen3-embedding-q5km","prompt":"test"}'

# 5. Run benchmark (requires indexed data — see below)
pytest tests/test_find_examples_recall.py -m ollama -v
\`\`\`
```

**Severity:** P2 (documentation only; test marked to skip without Ollama, will not block CI)

**Owner:** Documentation / Testing

---

### P2.2 — API Key Not Provided for Production Smoke Test

**Location:** Task scope (M7.5 Batch 4)

**Issue:** Production smoke test intended but no API key provisioned.

**Current state:**
- Production server is running and healthy (✅ `/health`)
- MCP endpoint `/mcp` enforces `X-API-Key` header authentication
- No test/public key documented in install page or CONTRIBUTING.md

**Gap:** Batch 4 agent cannot verify find_examples production behavior without authentication.

**Expected:** Either:
1. Admin provides a test API key (ephemeral, short-lived, rate-limited)
2. Or batch is reclassified as "requires admin SSH" (defer to separate admin sign-off)
3. Or smoke test uses local test environment instead (requires Ollama setup per P2.1)

**Severity:** P2 (cosmetic task blocker; smoke test is optional verification layer, not production gate)

**Owner:** Admin / Auth policy

---

## Recommendations

**To unblock Batch 4 in next iteration:**

1. **Short term (5 min):** Admin provides test API key → Batch 4 runs against production
2. **Medium term (30 min):** Document Ollama setup in CONTRIBUTING.md → Local integration test becomes self-service
3. **Long term:** Consider adding "dev/test" API key to `.env.example` with rate-limit=0 and `demo=true` flag for exactly this use case

**Current blocker severity:** P2 — Batch 3 infrastructure passed; Batch 4 is verification polish, not production gate. Safe to proceed to next batch/milestone with note in TASKS.md.

---

## Test Methodology (for when unblocked)

When infrastructure is available, smoke test will:

1. **Setup:** Activate venv, connect to Ollama + Neo4j + PostgreSQL
2. **Loop:** For each of 10 queries:
   - Call `_find_examples(query, odoo_version="17.0", limit=5)`
   - Parse top-5 entity names from response
   - Check if `expected_entity` substring appears in any top-5 result
   - Record: PASS (hit) or FAIL (miss)
3. **Score:** Count passes; gate ≥7/10 (proxy for full benchmark ≥0.70 recall@5)
4. **Report:** Document per-query hits + summary + any anomalies

---

**Report generated by:** Batch 4 recall smoke verification  
**Execution status:** Blocked (Ollama unavailable, API key not provided)  
**Next action:** Await admin provisioning of test API key or local Ollama setup  
**Escalation:** None (P2 issue, not production-critical)
