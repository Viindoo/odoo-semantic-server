# Multi-stage image for the indexer one-shot. Real CLI wired up in WP-6.

FROM python:3.11-slim AS builder

ENV UV_LINK_MODE=copy \
    UV_SYSTEM_PYTHON=1 \
    PYTHONDONTWRITEBYTECODE=1

RUN pip install --no-cache-dir uv

WORKDIR /src
COPY pyproject.toml ./
COPY uv.lock* ./
COPY osm ./osm

RUN uv sync --frozen --no-dev || uv sync --no-dev


FROM python:3.11-slim AS runtime

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PATH="/src/.venv/bin:$PATH"

WORKDIR /src

COPY --from=builder /src /src

# Placeholder. WP-6 lands scripts/index.py + a real CMD.
CMD ["python", "-c", "print('osm indexer: placeholder, implemented in WP-6')"]
