*.lock.*
.~lock.*
__pycache__/
.venv/
.serena/
odoo-semantic.conf
.env

# Claude Code local settings
.claude/

# Astro site build artifacts
site/node_modules/
site/dist/
site/.astro/
# uv.lock IS committed (issue #319): it is the exact dependency lock CI installs
# from via `uv sync --locked`. Do NOT ignore it.
site/node_modules

# Wave/agent scaffolding (worklogs, plan artifacts) - not product code
.odoo-ai/
